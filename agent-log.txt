Project: Project 13 — A Pipeline for an AI Application

Delegated task: Implement the complete repository pipeline, application, offline tests, reliability tooling, rollout controls, documentation, and local verification. Do not create REPORT.docx or RELIABILITY_MEMO.docx.

Human-supplied values:
- EVAL_GROUNDEDNESS_THRESHOLD = 0.90
- QUALITY_SLO = 0.99

Files inspected before editing:
- ci.yml
- judge.py
- error_budget.py
- psi.py
- C:\Users\Dipanjan\Desktop\Project 13 — A Pipeline for an AI Application.pdf (text extracted with pdftotext; the command emitted a MiKTeX log permission warning after output)

Files created or changed: The normalized application, config, corpus, evaluation, SRE, monitoring, rollout, deployment, scripts, unit tests, CI workflow, environment example, secret-scan config, ignore rules, README, NEXT_STEPS, and this log. The four root starter files were inspected and retained until cleanup.

Commands actually executed so far:
- rg --files -uu
- git status --short --branch (reported that the folder is not a Git repository)
- Get-Item on the assignment PDF
- Get-Content -Raw for all four starter files
- pdftotext -layout on the assignment PDF
- New-Item to create normalized directories
- python --version, Python executable discovery, pip version, and py launcher runtime discovery
- python -m venv .venv and pip install -r requirements.txt (first sandboxed creation failed; approved retry installed dependencies but the wrapper timed out after installation)
- ruff format ., ruff check . --fix, and repeated ruff check .
- pytest tests/unit -q (sandbox runs exposed temp-directory permission errors; approved retry completed)
- python -m compileall -q app evals sre monitoring rollout scripts
- python scripts/run_error_budget_demo.py
- python scripts/run_drift_demo.py
- import check for app.api
- python -m rollout.shadow
- python -m rollout.rollback (safely refused because there was no prior champion to restore)
- environment-presence checks without printing credential values
- ripgrep scans for unpinned model identifiers and common secret patterns
- gitleaks availability check (not installed locally; CI uses the pinned GitHub Action)
- workspace-local pytest temporary-directory cleanup after exact-path validation
- final ruff, pytest, compile, eval-case count, null-label count, DOCX absence, pinned-model, and secret-pattern acceptance scans
- git diff --stat (could not run because the supplied folder is not a Git repository)

Test failures and corrections:
- Ruff initially reported formatting, import, modernization, and line-length findings. Applied ruff formatting/autofixes and manually wrapped the two remaining long lines. Final ruff check passed.
- The first pytest run had 32 passes and one setup error because the sandbox denied pytest's user Temp directory. A workspace temp retry was also denied during cleanup. An approved offline retry outside that restriction passed all 33 tests.
- Direct execution of both demo scripts initially failed because their script directory, not the repository root, was on sys.path. Added explicit repository-root bootstrapping; both demos then succeeded.
- The first virtual environment creation failed in ensurepip. The exact partial path was verified, removed, and recreated with approval.
- Final verification passed ruff and all 33 unit tests. Compilation exited successfully; an earlier sandboxed compilation emitted nonfatal access warnings for inaccessible pytest temporary paths that were subsequently removed.

Assumptions:
- Each Markdown policy file is one deterministic retrieval chunk.
- Character count is used for question_length.
- The requested threshold 0.90 and SLO 0.99 are preserved exactly.
- No Git operations requiring a repository are performed because this folder is not a Git repository.

Remote work not completed: No GitHub authentication, Git remote, CI run, cloud evaluation result, or cloud deployment evidence was available or fabricated.
The local cloud evaluation was skipped because OPENAI_API_KEY and OPENAI_BASE_URL were not present. Local gitleaks execution was skipped because the executable is not installed; CI runs gitleaks/gitleaks-action@v2.

No human intervention recorded during this run beyond supplying the two required human-selected values.

Follow-up correction after user-reported cloud evaluation:
- The user reported 8/9 grounded cases, with `current-policy` failing because the generated answer inferred that last year's email process was no longer prescribed.
- Inspected `evals/cases.jsonl`, `data/policies/attendance.md`, the actual top-three lexical retrieval results, `app/rag.py`, `app/retrieval.py`, and the RAG unit tests.
- Root cause: the eval case declared the pre-2026 email method obsolete, but the retrieved corpus did not state that fact.
- Changed the fictional attendance policy to explicitly identify pre-2026 email instructions as no longer current, matching the existing case intent.
- Added a system-prompt rule prohibiting temporal discontinuation inferences unless context explicitly supports them.
- Added an offline regression test confirming attendance ranks first and the temporal claim is explicit in retrieved context.
- Executed ruff check, all unit tests, and compilation checks. Results: ruff passed, 34 tests passed, compilation passed.
- Did not run or fabricate another cloud evaluation.

Follow-up correction after the user-reported `lunch-menu` judge failure:
- The user reported another real 8/9 Ollama evaluation. The judge rejected the old abstention wording because it characterized the policies as insufficient rather than limiting itself to the supplied context.
- Inspected the unchanged `lunch-menu` case, canonical RAG abstention, strict judge rubric, RAG tests, judge tests, and reliability configuration.
- Changed the canonical abstention to: `I cannot answer that from the provided policy context.`
- Clarified that only a pure, claim-free abstention is grounded and added explicit grounded and ungrounded abstention examples. The general support rule remains strict, including for invented details appended to refusals.
- Added deterministic offline tests for a valid pure abstention, an abstention with an invented lunch claim, and a normal unsupported lunch claim.
- Executed ruff check, all unit tests, and compilation checks. Results: ruff passed, 37 tests passed, compilation passed.
- Preserved groundedness threshold 0.90 and quality SLO 0.99. Did not run or fabricate a cloud evaluation.
